{
  "name": "api-test",
  "version": "1.0.0",
  "description": "Backend service automation test implementation with reporting.",
  "author": "Ahmet Oğuz Ergin",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "clean": "cd C:\\Users\\lenovo\\Desktop\\repository\\repository\\Spring-Boot-Rest-Api\\database && docker-compose build db-initializer && docker-compose up -d db-initializer",
    "parallel": "mocha '**/*.spec.ts' --parallel --reporter mochawesome --require mochawesome/register --timeout 30000",
    "test": "cls && tsx ./node_modules/mocha/bin/_mocha '**/*.spec.ts' '**/sync.hook.ts' --reporter mochawesome --timeout 30000",
    "testClean": "cls && tsx ./node_modules/mocha/bin/_mocha '**/*.spec.ts' '**/sync.hook.ts' --reporter mochawesome --timeout 30000 && npm run clean",
    "spec": "cls && tsx ./node_modules/mocha/bin/_mocha '**/sync.hook.ts' '**/*init*.ts' '**/*user-project*.spec.ts' --reporter mochawesome --timeout 30000"
  },
  "keywords": [],
  "license": "ISC",
  "dependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.12.2",
    "axios": "^1.6.8",
    "mocha": "^10.8.2",
    "mochawesome": "^7.1.3",
    "tsx": "^4.19.2"
  }
}
